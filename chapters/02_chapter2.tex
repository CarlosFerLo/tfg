
\chapter{The Proposed Model}

Consider the presentation of a theory $U$.
The algorithm has one goal, given a derived $T$-rule 
$\dfrac{Q}{\Delta \text{ is a type}}$ 
of the theory $U$ it will try to create a term of type $\Delta$
under the context $Q$, i.e. finding a realization from 
the context $Q$ to the context $(Q, \Delta)$.
To do this we will consider the context category $C(U)_Q$,
to limit ourselves to reasoning under the context $Q$. 

Let $G$ be a finite subcategory of $C(U)_Q$ containing both $Q$ and $(Q, T)$.
We apply iteratively the following steps
\begin{enumerate}
    \item assign a label to all the objects of $G$
          in 3 categories: 'keep', 'expand' and 'delete'.
          (this is where the AI model comes in)
    \item Let $G_1$ be the category consisting of all objects of
          $G$ not labeled as 'delete'.
    \item For each object $X$ of $G$ labeled as 'expand' apply the following expansion rules:
        \begin{enumerate}
            \item 
        \end{enumerate}
\end{enumerate}

TODO: intoroducir $\leq$

TODO: operad